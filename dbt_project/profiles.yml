dbt_project:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: service-account
      project: $GCP_PROJECT_ID
      dataset: stg_olist
      threads: 4
      keyfile: $GCP_CREDENTIALS_PATH
      timeout_seconds: 300
      priority: interactive
      retries: 1
      location: US
